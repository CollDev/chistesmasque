<h1>Joke creation</h1>

<form action="{{ path('joke_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('joke') }}">
            Back to the list
        </a>
    </li>
</ul>
